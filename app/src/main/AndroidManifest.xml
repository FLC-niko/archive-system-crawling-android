<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <package android:name="com.tencent.mm" />
    </queries>

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name="com.topviewclub.crawling.core.base.CrawlApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:preserveLegacyExternalStorage="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Crawling"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".core.ui.CrawlingActivity"
            android:exported="true"
            android:launchMode="singleInstance"
            android:label="AAOS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            This device build is used for the official-account collector. The
            other feature modules remain available to the project, but their
            accessibility services must not be published into this APK: every
            service is shown as a separate "AAOS" entry by MIUI.
        -->
        <service
            android:name="com.topviewclub.crawling.wechat.video.VideoOperationService"
            tools:node="remove" />
        <service
            android:name="com.topviewclub.crawling.xuexi.XueXiOperationService"
            tools:node="remove" />
        <service
            android:name="com.topviewclub.crawling.wechat.auto.AutoChatOperationService"
            tools:node="remove" />
        <service
            android:name="com.topviewclub.crawling.service.wechat.check.CheckQRCodeOperationService"
            tools:node="remove" />
        <service
            android:name="com.topviewclub.crawling.service.wechat.weread.WeReadLoginOperationService"
            tools:node="remove" />
    </application>

</manifest>
